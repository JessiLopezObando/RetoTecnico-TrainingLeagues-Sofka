package com.retosofka.estacionespacial.entities;

import lombok.Data;

@Data
//@Entity
public class VehiculoLanzadera {

/*    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id_lanzadera")
    private Long id;

    @OneToOne
    @JoinTable(name = "tipo_nave", joinColumns = @JoinColumn(name = "id_Nave"))
    private List<Nave> tipoCarga;
    private Double pesoCarga;*/

    //Los constructores, getters y setters fueron creados con lombok

}
